import { DeleteContactFromGroupRequest } from './delete-contact-from-group-request';

describe('DeleteContactFromGroupRequest', () => {
  it('should create an instance', () => {
    expect(new DeleteContactFromGroupRequest()).toBeTruthy();
  });
});
