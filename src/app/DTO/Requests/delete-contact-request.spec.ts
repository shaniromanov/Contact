import { DeleteContactRequest } from './delete-contact-request';

describe('DeleteContactRequest', () => {
  it('should create an instance', () => {
    expect(new DeleteContactRequest()).toBeTruthy();
  });
});
