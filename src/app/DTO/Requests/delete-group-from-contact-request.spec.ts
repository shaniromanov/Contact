import { DeleteGroupFromContactRequest } from './delete-group-from-contact-request';

describe('DeleteGroupFromContactRequest', () => {
  it('should create an instance', () => {
    expect(new DeleteGroupFromContactRequest()).toBeTruthy();
  });
});
