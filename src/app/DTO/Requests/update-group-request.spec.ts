import { UpdateGroupRequest } from './update-group-request';

describe('UpdateGroupRequest', () => {
  it('should create an instance', () => {
    expect(new UpdateGroupRequest()).toBeTruthy();
  });
});
