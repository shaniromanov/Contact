import { UpdateGroupResponseOk } from './update-group-response-ok';

describe('UpdateGroupResponseOk', () => {
  it('should create an instance', () => {
    expect(new UpdateGroupResponseOk()).toBeTruthy();
  });
});
