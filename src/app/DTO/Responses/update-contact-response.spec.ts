import { UpdateContactResponse } from './update-contact-response';

describe('UpdateContactResponse', () => {
  it('should create an instance', () => {
    expect(new UpdateContactResponse()).toBeTruthy();
  });
});
