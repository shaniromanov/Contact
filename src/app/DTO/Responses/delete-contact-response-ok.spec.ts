import { DeleteContactResponseOk } from './delete-contact-response-ok';

describe('DeleteContactResponseOk', () => {
  it('should create an instance', () => {
    expect(new DeleteContactResponseOk()).toBeTruthy();
  });
});
