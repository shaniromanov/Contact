import { RegisterUserResponseOk } from './register-user-response-ok';

describe('RegisterUserResponseOk', () => {
  it('should create an instance', () => {
    expect(new RegisterUserResponseOk()).toBeTruthy();
  });
});
