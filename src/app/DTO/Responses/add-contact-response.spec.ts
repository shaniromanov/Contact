import { AddContactResponse } from './add-contact-response';

describe('AddContactResponse', () => {
  it('should create an instance', () => {
    expect(new AddContactResponse()).toBeTruthy();
  });
});
