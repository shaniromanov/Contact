import { RegisterUserResponseUsernameExists } from './register-user-response-username-exists';

describe('RegisterUserResponseUsernameExists', () => {
  it('should create an instance', () => {
    expect(new RegisterUserResponseUsernameExists()).toBeTruthy();
  });
});
