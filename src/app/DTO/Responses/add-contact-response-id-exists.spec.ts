import { AddContactResponseIdExists } from './add-contact-response-id-exists';

describe('AddContactResponseIdExists', () => {
  it('should create an instance', () => {
    expect(new AddContactResponseIdExists()).toBeTruthy();
  });
});
