import { LoginResponseNotExists } from './login-response-not-exists';

describe('LoginResponseNotExists', () => {
  it('should create an instance', () => {
    expect(new LoginResponseNotExists()).toBeTruthy();
  });
});
