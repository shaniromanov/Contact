import { DeleteContactResponse } from './delete-contact-response';

describe('DeleteContactResponse', () => {
  it('should create an instance', () => {
    expect(new DeleteContactResponse()).toBeTruthy();
  });
});
