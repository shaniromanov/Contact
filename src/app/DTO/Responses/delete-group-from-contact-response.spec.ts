import { DeleteGroupFromContactResponse } from './delete-group-from-contact-response';

describe('DeleteGroupFromContactResponse', () => {
  it('should create an instance', () => {
    expect(new DeleteGroupFromContactResponse()).toBeTruthy();
  });
});
