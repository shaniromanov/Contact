import { DeleteGroupResponse } from './delete-group-response';

describe('DeleteGroupResponse', () => {
  it('should create an instance', () => {
    expect(new DeleteGroupResponse()).toBeTruthy();
  });
});
