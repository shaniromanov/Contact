import { AddContactResponseOk } from './add-contact-response-ok';

describe('AddContactResponseOk', () => {
  it('should create an instance', () => {
    expect(new AddContactResponseOk()).toBeTruthy();
  });
});
