import { RegisterUserResponse } from './register-user-response';

describe('RegisterUserResponse', () => {
  it('should create an instance', () => {
    expect(new RegisterUserResponse()).toBeTruthy();
  });
});
